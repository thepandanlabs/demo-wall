{
  "name": "demo-wall",
  "version": "1.0.0",
  "private": true,
  "description": "DEMOWALL — a curated wall of technology demos, hosted on Netlify.",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'static site, no build step'",
    "seed": "node netlify/functions/_lib.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^19.0.0"
  }
}