# DEMOWALL environment variables
# Copy to .env for local development (netlify dev picks it up automatically).
# On Netlify: Site settings > Environment variables, or use `netlify env:set`.

# Required. The password for the /admin panel.
# Choose something long and random.
DEMO_WALL_PASSWORD=change-me-to-a-long-random-password